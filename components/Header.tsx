import React from "react";

const Header = () => {
  return (
    <header className="container mx-auto backdrop-blur-3xl bg-black/10 h-16 flex items-center rounded-full fixed inset-0 top-6 px-4 z-50">
      <span className="font-rubik-sans text-xl font-semibold">LOGO</span>
    </header>
  );
};

export default Header;
